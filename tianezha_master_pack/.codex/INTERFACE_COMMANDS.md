# INTERFACE:// Notes

INTERFACE:// identity.loadProfile(address, arenaSlug)
INTERFACE:// heartbeat.chooseActiveAgents(epochId)
INTERFACE:// markets.placeSimPosition(profileId, marketId, side, size)
INTERFACE:// governance.createVoteRequest(profileId, epochId, side)
INTERFACE:// governance.recordVerifiedVote(requestId, txHash)
