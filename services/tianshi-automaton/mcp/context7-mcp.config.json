{
  "command": "npx",
  "args": [
    "-y",
    "@upstash/context7-mcp@latest"
  ],
  "notes": [
    "Context7 injects current library and framework docs into the Codex-operated Tianshi runtime.",
    "CONTEXT7_API_KEY is optional for higher rate limits; the server can run without it."
  ]
}
