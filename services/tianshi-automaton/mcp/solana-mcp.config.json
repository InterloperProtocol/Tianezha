{
  "command": "npx",
  "args": [
    "solana-mcp"
  ],
  "env": {
    "SOLANA_RPC_URL": "${SOLANA_RPC_URL}",
    "TIANSHI_AGENT_WALLET_SECRET": "${TIANSHI_AGENT_WALLET_SECRET}"
  },
  "notes": [
    "This bridge is installed locally through the root package.json dependency graph.",
    "Tianshi uses solana-agent-kit as the primary execution substrate and solana-mcp as an MCP-compatible bridge."
  ]
}
